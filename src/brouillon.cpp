/*#include "piece.hpp"
fuioejhfioqedqz test
int main() {
    // Dimensions de la grille
    const int gridWidth = 10;
    const int gridHeight = 10;

    // Crée une grille
    Grid grid(gridWidth, gridHeight);

    // Crée une pièce (par exemple, une pièce en T)
    Piece piece('L', 2, 2);

    // Réinitialise la grille
    grid.empty();

    // Place la pièce dans la grille
    grid.placePiece(piece);

    // Affiche la grille
    grid.printGrid();


    piece.rotate();
    // Réinitialise la grille
    grid.empty();

    // Place la pièce dans la grille
    grid.placePiece(piece);

    // Affiche la grille
    grid.printGrid();

    piece.rotate();
    // Réinitialise la grille
    grid.empty();

    // Place la pièce dans la grille
    grid.placePiece(piece);

    // Affiche la grille
    grid.printGrid();

    piece.rotate();
    // Réinitialise la grille
    grid.empty();

    // Place la pièce dans la grille
    grid.placePiece(piece);

    // Affiche la grille
    grid.printGrid();

    piece.rotate();
    // Réinitialise la grille
    grid.empty();

    // Place la pièce dans la grille
    grid.placePiece(piece);

    // Affiche la grille
    grid.printGrid();

    return 0;
}
*/